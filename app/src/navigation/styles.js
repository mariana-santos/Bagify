import { StyleSheet } from "react-native"

export default styles = StyleSheet.create({
    tabBar: {
      backgroundColor: '#f0f',
      borderTopColor: '#ccc',
      borderTopWidth: 1,
    },
    tabBarItem: {
      justifyContent: 'center',
      alignItems: 'center',
      paddingVertical: 8,
      width: '33%',
    },
  });
  